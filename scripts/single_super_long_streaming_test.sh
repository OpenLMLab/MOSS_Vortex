date
curl -X POST http://127.0.0.1:7000/inference -d '{"x": "MOSS is an AI assistant developed by the FudanNLP Lab and Shanghai AI Lab. Below is a conversation between MOSS and human. [Human]: hello !<eoh> [MOSS]: hello! <eoa> [Human]: Can you tell me the java code of quicksort ?<eoh> [MOSS]:The Java code of Quicksort can be found here: public static void quickSort(int[] array, int low, int high) {if (low < high) {int pi = partition(array, low, high);quickSort(array, low, pi-1);quickSort(array, pi+1, high);} } public static int partition(int[] array, int low, int high) {int pivot = array[high];int i = low - 1;for (int j=low; j<high; j++) {if (array[j] <= pivot) {i++;array[i] = array[j];}}return i + 1; } <eoa> [Human]: Great! But can you tell me the python code of quicksort? <eoh> Sure! The Python code of Quicksort looks like this: def quicksort(arr): if len(arr) <= 1: return arr pivot = arr[len(arr) // 2] left = [x for x in arr if x < pivot] middle = [x for x in arr if x == pivot] right = [x for x in arr if x > pivot] return quicksort(left) + middle + quicksort(right) <eoa> [Human]: That is awesome! Thanks. <eoh> [Human]: hello!<eoh> [MOSS]: hello! <eoa> [Human]: Can you tell me the java code of quicksort?<eoh> [MOSS]:The Java code of Quicksort can be found here: public static void quickSort(int[] array, int low, int high) {if (low < high) {int pi = partition(array, low, high);quickSort(array, low, pi-1);quickSort(array, pi+1, high);} } public static int partition(int[] array, int low, int high) {int pivot = array[high];int i = low - 1;for (int j=low; j<high; j++) {if (array[j] <= pivot) {i++;array[i] = array[j];}}return i + 1; } <eoa> [Human]: Great! But can you tell me the python code of quicksort? <eoh> Sure! The Python code of Quicksort looks like this: def quicksort(arr): if len(arr) <= 1: return arr pivot = arr[len(arr) // 2] left = [x for x in arr if x < pivot] middle = [x for x in arr if x == pivot] right = [x for x in arr if x > pivot] return quicksort(left) + middle + quicksort(right) <eoa> [Human]: That is awesome! Thanks. <eoh> You are welcome! I am always happy to help.<eoa> [Human]: That is awesome! Thanks. <eoh>", "top_k":5}'
date